# API Gateway endpoint (ví dụ: https://xxxxxx.execute-api.ap-southeast-1.amazonaws.com/prod)
API_GATEWAY_URL=

# JWT secret dùng cho xác thực backend
JWT_SECRET=

# Port backend Express
PORT=

# Các biến khác nếu cần (ví dụ: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY nếu backend cần gọi AWS trực tiếp)