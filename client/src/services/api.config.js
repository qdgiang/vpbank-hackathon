const baseURL = '/api/v1';

const apiConfig = {
  baseURL,
  users: '/user',
  transactions: '/transaction',
  notifications: '/notification',
  goals: '/goal',
  jars: '/jar',
  login: '/auth/login',
};

export default apiConfig; 